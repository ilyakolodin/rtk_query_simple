import React from 'react';
import './App.css';
import { useAppSelector } from './hooks/redux'

function App() {
  const {increment} = userSlice.actions
  const dispatch = 
  return (
    <div className="App">
    Hello!!!
    </div>
  );
}

export default App;
